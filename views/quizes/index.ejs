<h3>Buscador de preguntas</h3>
<form method="get" action="/quizes">
    <input type="text" name="search" placeholder="buscar pregunta" required/>
    <input type="submit" value="Buscar"/>
</form>

<% if(quizes.length == 0) {%><p>No encontrado</p><% } %>

<table>
<caption>Lista de preguntas:</caption>
<% var i; for (i=0; i < quizes.length; i++) { %>
   <tr>
    <td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
    <td><a href="quizes/<%= quizes[i].id %>/edit"><button type="button">Editar</button></a></td>
    <td>
      <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
        <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
          Borrar
        </button>
      </form>
    </td>
   </tr>
<% } %>
</table>

<a href="/quizes/new"><button type="button">Crear pregunta</button></a>
