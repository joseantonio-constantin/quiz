<p>
  <%= respuesta %>
  <% if(respuesta === 'Correcto') { %>
    <a href="/quizes/<%= quiz.id %>" class="btn btn-success">Volver</a>
  <% } else { %>
    <a href="/quizes/<%= quiz.id %>" class="btn btn-danger">Volver</a>
  <% } %>
</p>
